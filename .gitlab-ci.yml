pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r lethalcompany public/lethalcompany
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
